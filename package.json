{
  "name": "test",
  "version": "1.0.0",
  "description": "test",
  "main": "index.js",
  "scripts": {
    "start": "rimraf dist && npm run build && node ./dist/index.js",
    "build": "tsc -p ./",
    "test": "rimraf dist && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "abort-controller": "^3.0.0",
    "jsdom": "^15.2.1",
    "mkdirp-promise": "^5.0.1",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/jsdom": "^12.2.4",
    "@types/mkdirp-promise": "^5.0.0",
    "@types/node": "^12.12.8",
    "@types/node-fetch": "^2.5.3",
    "rimraf": "^3.0.0"
  }
}
